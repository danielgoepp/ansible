---
# This playbook upgrades Open WebUI on adambalm using the LLM role
# The LLM role handles both initial installation and upgrades
# For initial setup, use playbooks/ssh/setup-adambalm.yaml which includes the full LLM role

- name: Upgrade Open WebUI on adambalm
  hosts: adambalm
  become: true
  gather_facts: false

  tasks:
    - name: Include Open WebUI tasks from LLM role
      ansible.builtin.include_role:
        name: llm
        tasks_from: openwebui
